name: Apply Slack List diagnostics

on:
  push:
    paths:
      - '.github/workflows/apply-slack-list-column-fallback.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Add Slack API error context
        run: python .github/scripts/patch_slack_api_error_context.py
      - name: Commit patch
        run: |
          git config user.name "A2O Automation"
          git config user.email "a2ogrouphk@gmail.com"
          git add app/api/_lib/slackLeadList.ts
          git commit -m "chore: include Slack List API method in errors"
          git push origin HEAD:main
